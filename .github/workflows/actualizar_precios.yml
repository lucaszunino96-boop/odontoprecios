name: Actualizar precios

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Correr scraper
        run: python scraper.py

      - name: Subir productos.json
        run: |
          git config user.name "OdontoPrecio Bot"
          git config user.email "bot@odontoprecios.onrender.com"
          git add productos.json || true
          git add historial.json || true
          # El comando 'pull --rebase' trae los cambios de GitHub y pone los tuyos arriba
          git pull --rebase origin main
          git diff --staged --quiet || git commit -m "Actualizacion automatica $(date '+%d/%m/%Y %H:%M')"
          git push origin main
